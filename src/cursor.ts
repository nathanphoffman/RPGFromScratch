
export function setupCursor() {
/*
    // Get the sword cursor element
    const cursor = document.getElementById('cursor');
    if(!cursor) return;

    // Track mouse movement and update cursor position
    document.addEventListener('mousemove', (e) => {
        const offset = 0; // Half of cursor size (50px / 2) – updated for BIG cursor

        // Calculate centered position based on mouse location and offset
        const x = Math.round(e.pageX - offset);
        const y = Math.round(e.pageY - offset);

        // Update custom CSS variables for animation and transform
        cursor.style.setProperty('--mouse-x', `${x}px`);
        cursor.style.setProperty('--mouse-y', `${y}px`);

        // Move the cursor element to follow the mouse
        cursor.style.transform = `translate3d(${x}px, ${y}px, 0)`;
    });


    // Prevent default right-click menu (for clean interaction)
    document.addEventListener("contextmenu", e => e.preventDefault());
*/
}